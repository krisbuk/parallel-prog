# Лабораторная работа №1
* Выполнила: Букаускайте Кристина Витальевна
* Группа: 6213-100503D

### Реализованный функционал:
1. **`lab1.cpp`** Читает матрицы из файлов, выполняет вычисления по классическому алгоритму, фиксирует время работы и объем задачи, сохраняет результаты.
2. **`generate.py` (Python)**: Автоматическая генерации тестовых данных. Создает квадратные матрицы заданного размера, заполняя их случайными вещественными числами, и сохраняет их в `matrixA.txt` и `matrixB.txt`.
3. **Python Верификация (`verify.py`):** Автоматически вызывается из C++ кода, проверяет корректность вычислений с помощью библиотеки `NumPy`.
4. **Python Бенчмарк (`plot_perf.py`):** Оценивает производительность системы в GFLOPS при изменении размера задачи.

Были умножены матрицы из файлов `matrixA.txt` и `matrixB.txt`, размером **500 x 500**. Проверка пройдена, вычисления корректны. Результаты вычислений, а так же информация о скорости вычисления умножения были сохранены в файл `result.txt`

---

## Анализ производительности

В ходе работы был проведен анализ эффективности алгоритма на различных объемах данных (от 10 x 10 до 300 x 300). Результаты замера производительности в **GFLOPS** представлены на графике ниже:

![Matrix Multiplication Performance](lab1/performance.png)

### Выводы по графику:

* Размеры от 10 до 100: Наблюдается резкий, практически линейный рост производительности с 0.0065 GFLOPS до пикового значения около 0.0118 GFLOPS.
* Размеры от 100 до 300: График стабилизируется и переходит в горизонтальное плато, колеблясь в узком диапазоне 0.0116–0.0121 GFLOPS.

**Итог:** Классический алгоритм ($i \rightarrow j \rightarrow k$) показывает нестабильную скорость работы на матрицах крупного размера. При этом общая физическая тенденция алгоритма — стабилизация и насыщение пропускной способности памяти при росте размера задачи — полностью подтверждается полученными экспериментальными данными.

---
---

### Фактические показатели для размера 500x500 (содержится в начале файла `result.txt`):
Execution stats ->

* Problem Size (Dimension N) : 500 x 500
* Total Elements per Matrix  : 250000
* Number of Multiplications  : 125000000
* Execution Time             : 2792.8389 ms

## Окружение и тестовый запуск

Все замеры производительности проводились на следующей конфигурации аппаратного и программного обеспечения:
* **CPU:** i7-13620H
* **RAM:** 16 GB
* **Операционная система:** Windows 11
* **Среда разработки:** Visual Studio 2026

### Требования:
* Компилятор C++
* Python 3.x + библиотеки `numpy`, `matplotlib`
